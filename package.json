{
  "name": "openharness",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered engineering governance framework — spec-driven change lifecycle + harness execution",
  "license": "Apache-2.0",
  "scripts": {
    "build": "pnpm -r run build",
    "lint": "eslint packages/*/src/**/*.ts",
    "typecheck": "pnpm -r run typecheck",
    "test": "pnpm -r run test",
    "cli": "pnpm --filter @openharness/core run cli"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0",
    "eslint": "^9.0.0",
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.0"
}
